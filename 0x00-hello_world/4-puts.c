#include<stdio.h>

/**
 * main -prints a given phrase
 * Return: Always 0 if success, non zero otherwise
 */
int main(void)
{
puts("\"Programming is like building a multlingual puzzle");
return (0);
}
